export const GameConfig = {
  /**
   * 旋转靶半径
   */
  gameTargetRadius: 100,
  /**
   * 旋转靶 y 坐标
   */
  gameTargeY: 500,
  /**
   * 箭高度
   */
  arrowHeight: 200,
  /**
   * 箭宽度
   */
  arrowWidth: 8,
  /**
   * 箭把半径
   */
  arrowBallRadius: 80,
} as const
