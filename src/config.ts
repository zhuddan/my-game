export const GameConfig = {
  /**
   * 旋转靶半径
   */
  gameTargetRadius: 200,
  /**
   * 箭高度
   */
  arrowHeight: 200,
  /**
   * 箭宽度
   */
  arrowWidth: 8,
  /**
   * 箭把半径
   */
  arrowBallRadius: 20,
} as const
